import { MarksCorrectionDirective } from './marks-correction.directive';

describe('MarksCorrectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MarksCorrectionDirective();
    expect(directive).toBeTruthy();
  });
});
